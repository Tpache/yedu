<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.com.yedu.dao.IMenuDao">

	<select id="generateMenu" resultType="java.util.Map">
		SELECT
			t.ID as 'id',
			t.menuId as menuId,
			t.`name` as 'name',
			t.type as type,
			t.url as url
		FROM
			t_menu t
		WHERE
			t.appId = #{0} 
			<if test="#{1} == null || #{1} == '' ">
				AND t.parentId = '-1' 
			</if>
			<if test="#{1} != null || #{1} != '' ">
				AND t.parentId = #{1} 
			</if>
		ORDER BY
			t.menuId
	</select>

</mapper>